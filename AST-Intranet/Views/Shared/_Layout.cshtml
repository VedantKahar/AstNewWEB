<!-- Views/Shared/_Layout.cshtml -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <link rel="stylesheet" href="~/Content/css/HeaderFooterStyles.css" />
</head>
<body>

    <!-- Render Header -->
    @Html.Partial("_Header")

    <div class="main-content">
        <!-- Content of the page will go here -->
        <div class="content">
            @RenderBody() <!-- This will render the content from the current view -->
        </div>

        <!-- Render Footer -->
        @Html.Partial("_Footer")
    </div>

    <!-- Inline JavaScript -->
    <!--<script>
        // Function to fetch suggestions based on the input text
        function fetchSuggestions(query) {
            if (query.length < 1) {
                document.getElementById('suggestions-box').style.display = 'none';
                return;
            }

            // Perform AJAX request to fetch search suggestions
            fetch(`/Search/GetSuggestions?q=${encodeURIComponent(query)}`)
                .then(response => response.json())
                .then(data => {
                    // Clear previous suggestions
                    const suggestionsBox = document.getElementById('suggestions-box');
                    suggestionsBox.innerHTML = '';

                    // If no results, hide the box
                    if (data.length === 0) {
                        suggestionsBox.style.display = 'none';
                        return;
                    }

                    // Show suggestions box
                    suggestionsBox.style.display = 'block';

                    // Populate the suggestions box
                    data.forEach(suggestion => {
                        const div = document.createElement('div');
                        div.classList.add('suggestion-item');
                        div.textContent = suggestion.title;
                        div.onclick = () => window.location.href = suggestion.url; // Navigate to the selected item
                        suggestionsBox.appendChild(div);
                    });
                })
                .catch(error => console.error('Error fetching suggestions:', error));
        }
    </script>-->
</body>
</html>
